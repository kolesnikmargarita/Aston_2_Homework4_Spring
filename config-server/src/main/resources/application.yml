server:
  port: 8888 # Стандартный порт для Config Server

spring:
  application:
    name: config-server
  profiles:
    active: native #искать конфиги в локальной файловой системе
  cloud:
    config:
      server:
        native:
          search-locations: file:D:/microservices-config/
